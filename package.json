{
  "name": "capacitor-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"Skipping web build (static www)\"",
    "android": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.1.2",
    "@capacitor/local-notifications": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
